import Identifiable from "../Identifiable";

export default class CharacterCategory extends Identifiable {
    public name: string = "";
    public description: string = "";
    public color: string = "";
}