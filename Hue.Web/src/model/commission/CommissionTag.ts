import Identifiable from "../Identifiable";

export default class CommissionTag extends Identifiable {
    public name: string = "";
    public description: string = "";
    public color: string = "";
}